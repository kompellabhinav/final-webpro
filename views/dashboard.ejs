<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="stylesheet" href="css/dashboard.css">
    <link rel="icon" href="../utilities/logo.png" type="image/icon type">
</head>
<body>
    <div class="logo">
      <img src="../utilities/logo.png" alt="Logo">
    </div>
    <h1 class="uname"><%= newListItems.fullName %></h1>
    <h1 class="title">Workout Tracker</h1>
    <div class="tbl-header">
      <table cellpadding="0" cellspacing="0">
        <thead>
          <tr>
            <th>ID</th>
            <th>Workout Name</th>
            <th>Body</th>
            <th>Sets</th>
            <th>Max Weight</th>
          </tr>
        <% for(var i = 0; i < newListItems.workout.length; i++) { %>
            <tr>
                <th><%= i+1 %></th>
                <th><%= newListItems.workout[i].name %></th>
                <th><%= newListItems.workout[i].body %></th>
                <th><%= newListItems.workout[i].sets %></th>
                <th><%= newListItems.workout[i].maxWeight %></th>
            </tr>
        <% } %>
            <tr>
              <form action="/dashboard" method="post">
                <th>
                  <button type="submit" class="aButton">Add</button>
                </th>
                <th><input type="text" name="newName" placeholder="Name"></th>
                <th><input type="text" name="newBody" placeholder="Body"></th>
                <th><input type="text" name="newSet" placeholder="Set"></th>
                <th><input type="text" name="newWeight" placeholder="Max Weight"></th>
                
              </form> 
            </tr>
        </thead>
      </table>
    </div>
</body>
</html>